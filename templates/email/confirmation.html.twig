<h3>
    Hello {{ user.getFullName() }}, please confirm your e-mail address.
</h3>

<p>
    Please confirm your e-mail address by clicking
    <a href="{{ url('default_confirmation_token', {'token': user.getConfirmationToken()}) }}">this</a>
    link or, type the code inside the form: <b>{{ user.getConfirmationToken() }}</b>.
</p>